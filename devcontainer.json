{
  "name": "Friko Recomienda",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "pip3 install --user -r requirements.txt && echo '✅ Dependencias instaladas'",
  "postStartCommand": "echo 'Para correr la app: python3 -m streamlit run app.py'",
  "forwardPorts": [8501],
  "portsAttributes": {
    "8501": {
      "label": "Streamlit App",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "secrets": {
    "ANTHROPIC_API_KEY": {
      "description": "Tu API key personal de Anthropic (https://console.anthropic.com/settings/keys)",
      "documentationUrl": "https://docs.anthropic.com/en/docs/initial-setup#create-an-api-key"
    }
  }
}